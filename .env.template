# Alumni Platform Environment Configuration
# Copy this file to .env and update with your values

# === CORE APPLICATION SETTINGS ===
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=your-super-secret-key-change-in-production

# === DATABASE SETTINGS ===
# For PostgreSQL (production)
# POSTGRES_URL=postgresql://user:password@host:5432/database

# For SQLite (development) - leave empty to use default
# DATABASE_URL=

# === SUPER ADMIN ACCOUNT ===
# These will create the initial super admin user
SUPER_ADMIN_EMAIL=admin@yourplatform.com
SUPER_ADMIN_PASSWORD=your-secure-admin-password

# === EMAIL CONFIGURATION ===
# Gmail SMTP settings (recommended for development)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_ADDRESS=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_USE_TLS=true
EMAIL_FROM_NAME=Alumni Platform
BASE_URL=http://localhost:5173

# === CORS SETTINGS ===
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# === OPTIONAL SETTINGS ===
# Redis (for caching and task queues)
# REDIS_URL=redis://localhost:6379/0

# File upload settings
# MAX_CONTENT_LENGTH=16777216  # 16MB
# UPLOAD_FOLDER=uploads

# === NOTES ===
# 1. For Gmail, use an App Password instead of your regular password
# 2. Create App Password: https://support.google.com/accounts/answer/185833
# 3. Never commit the actual .env file to version control
# 4. In production, use environment variables instead of .env file
